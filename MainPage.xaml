<Page
    x:Class="SimpleClock.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleClock"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    RequestedTheme="Dark">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <CommandBar Height="40" Grid.Row="0"  HorizontalAlignment="Stretch" OverflowButtonVisibility="Collapsed" >
            <AppBarButton Icon="Setting">
                <AppBarButton.Flyout>
                    <Flyout>
                        <StackPanel>
                            <TextBlock Text="Clock Size:" VerticalAlignment="Bottom" HorizontalAlignment="Left" />
                            
                        </StackPanel>
                    </Flyout>
                </AppBarButton.Flyout>
            </AppBarButton>
            <AppBarButton Icon="FullScreen" Click="FullScreen_Button_Click"/>
        </CommandBar>
        <TextBlock Text="1:20 PM" VerticalAlignment="Center" HorizontalAlignment="Center" Width="Auto" Height="Auto" Margin="10,10,10,10" FontSize="120" />
    </Grid>
</Page>
